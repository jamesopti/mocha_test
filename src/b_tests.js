const expect = require('expect.js');

describe('Suite B - 5 tests total', function() {
  describe('B', function() {
    it('B.t1', function() {
      expect(1).to.eql(0);
    });
    it('B.t2', function() {
      expect(1).to.eql(0);
    });
    it('B.t3', function() {
      expect(1).to.eql(0);
    });
    it('B.t4', function() {
      expect(1).to.eql(0);
    });
    it('B.t5', function() {
      expect(1).to.eql(0);
    });
  });
});
