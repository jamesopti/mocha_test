const expect = require('expect.js');

describe('Suite B - 10 tests total', function() {
  describe('B', function() {
    it('B.t1', function() {
      expect(1).to.eql(0);
    });
    it('B.t2', function() {
      expect(1).to.eql(0);
    });
    it('B.t3', function() {
      expect(1).to.eql(0);
    });
    it('B.t4', function() {
      expect(1).to.eql(0);
    });
    it('B.t5', function() {
      expect(1).to.eql(0);
    });
    it('B.t6', function() {
      expect(1).to.eql(0);
    });
    it('B.t7', function() {
      expect(1).to.eql(0);
    });
    it('B.t8', function() {
      expect(1).to.eql(0);
    });
    it('B.t9', function() {
      expect(1).to.eql(0);
    });
    it('B.t10', function() {
      expect(1).to.eql(0);
    });
  });
});
